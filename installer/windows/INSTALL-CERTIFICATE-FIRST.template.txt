AES PROGRAMMER & TROUBLESHOOTER - OPTIONAL TEST CERTIFICATE

This package was signed with a self-signed test certificate. Public production
releases should use a certificate issued by a trusted code-signing authority.

Certificate file: {CERTIFICATE_FILE}
Subject: {CERTIFICATE_SUBJECT}
Thumbprint: {CERTIFICATE_THUMBPRINT}
Certificate SHA-256: {CERTIFICATE_SHA256}
Valid from: {CERTIFICATE_NOT_BEFORE}
Valid until: {CERTIFICATE_NOT_AFTER}

Installer: {INSTALLER_FILE}
FTDI dependency SHA-256: {FTDI_SHA256}

For a controlled test computer only:

1. Right-click the `.cer` file and choose Install Certificate.
2. Choose Local Machine and approve the administrator prompt.
3. Place it in Trusted Root Certification Authorities and Trusted Publishers.
4. Run the installer.

The installer can also offer to trust this certificate when it is packaged with
the release. Remove the certificate from both stores after testing if it should
no longer be trusted.

No API key or credential file is included. Users obtain their own Geoapify key
at https://myprojects.geoapify.com/ and enter it inside the app.
