# Superior AES Programmer for Android credential template
# Copy this file outside the source tree, replace only the *** placeholders,
# edit it before distribution, import it from Configuration, and then protect it.
# Populated values are migrated to Android Secure Storage and never printed back.
# Values must never be logged, exported, committed, or bundled into an APK.

GeoapifyApiKey=***
Company1ApiKey=***
Company2ApiKey=***
CompanyPasscode=***
