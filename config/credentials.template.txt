# Superior AES Programmer local credential template
#
# This file is safe to distribute because it contains placeholders only.
# Keep this file named credentials.local.txt beside the setup EXE.
# Windows setup imports that external file into the preferred machine-wide
# location below. Replace only the placeholder before distribution. Setup
# preserves the installed local file during uninstall.
#
# Preferred machine-wide location:
#   %ProgramData%\SuperiorFire\AES Programmer\credentials.local.txt
#
# Installed application fallback:
#   <application folder>\config\credentials.local.txt

GeoapifyApiKey=***

# Add future application credentials only when a documented app feature names
# the exact variable. Unknown names are ignored. Never store AES subscriber
# cipher values, panel programming codes, or other site-specific passcodes here.
